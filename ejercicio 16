numero = int(input("Introduce un número entero positivo: "))
if numero <= 0:
  print("El número debe ser entero positivo.")
else:
  # Iniciar una lista para almacenar los números impares
  impares = []
  for i in range(1, numero + 1, 2):
    impares.append(str(i)) 
print(", ".join(impares))
